// Stamped with the git commit this was built from, so the popup can show
// which version is actually loaded (useful since this extension has no
// build step - reloading in chrome://extensions is the only way to pick
// up changes, and it's easy to think you've reloaded when you haven't).
//
// This can't contain the hash of the commit that introduces this exact
// file content (a commit's hash depends on its own tree, so a file can't
// reference its own commit's hash) - it's updated in a small follow-up
// commit after each real change, so it lags by one (metadata-only) commit
// at most.
export const GIT_COMMIT = "79824ae";
export const STAMPED_AT = "2026-09-22T08:20:42-07:00";
